#this class must not be checked in
---

spring:
  config:
    activate:
      on-profile: external_keys

app:
  models:
    openai:
      api-connection:
        key: NOT_NEEDED
    anthropic:
      api-connection:
        key: NOT_NEEDED